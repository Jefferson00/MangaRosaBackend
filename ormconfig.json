{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "mangarosa_db",
  "password": "mangarosa_db_password",
  "database": "mangarosa_db",
  "entities": [
    "./dist/models/**/*.entity.{ts,js}"
  ],
  "migrations": [
    "./dist/database/migrations/*.{ts,js}"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations/"
  }
}